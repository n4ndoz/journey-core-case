# journey-core-case

AINA Health `journey-core` technical challenge.

## Bootstrap

The project uses Python 3.12, FastAPI, Pydantic v2, and pytest.

Run the test suite with:

```bash
python -m pytest
```

Run the API locally with:

```bash
uvicorn app.main:app --reload
```

Health check: `GET /health`
